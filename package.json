{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prebuild": "npm install",
    "build": "lerna run build",
    "prepublish": "npm run build",
    "publish": "lerna publish from-git"
  },
  "devDependencies": {
    "jest": "^29.4.3",
    "lerna": "^6.5.1",
    "typescript": "^4.9.5"
  }
}
